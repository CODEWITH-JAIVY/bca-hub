import React from 'react';

const Chapter1_HTMLAndCSSBasics = () => {
    return (
        <div>
            <h2>Chapter 1: HTML and CSS Basics</h2>
            <p>
                <strong>Q1: What is HTML?</strong>
                <br />
                A1: HTML (HyperText Markup Language) is the standard markup language for creating web pages, providing the structure and content of a webpage.
            </p>
            <p>
                <strong>Q2: What are the basic HTML tags?</strong>
                <br />
                A2: Basic tags include <code>&lt;html&gt;</code>, <code>&lt;head&gt;</code>, <code>&lt;title&gt;</code>, <code>&lt;body&gt;</code>, <code>&lt;h1&gt;</code> to <code>&lt;h6&gt;</code>, <code>&lt;p&gt;</code>, <code>&lt;a&gt;</code>, <code>&lt;img&gt;</code>, <code>&lt;ul&gt;</code>, <code>&lt;ol&gt;</code>, and <code>&lt;li&gt;</code>.
            </p>
            <p>
                <strong>Q3: What is CSS?</strong>
                <br />
                A3: CSS (Cascading Style Sheets) is a style sheet language used for describing the look and formatting of a document written in HTML.
            </p>
            <p>
                <strong>Q4: How do you include CSS in HTML?</strong>
                <br />
                A4: CSS can be included using inline styles, internal styles (<code>&lt;style&gt;</code> tag), or external stylesheets (<code>&lt;link&gt;</code> tag).
            </p>
            <p>
                <strong>Q5: What are CSS selectors?</strong>
                <br />
                A5: CSS selectors are patterns used to select the HTML elements you want to style, such as element selectors, class selectors, and ID selectors.
            </p>
        </div>
    );
};

export default Chapter1_HTMLAndCSSBasics;
